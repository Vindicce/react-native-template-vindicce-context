interface I{{ pascalCase name }}Factory {
  showName: string;
}

function create{{ pascalCase name }}(): any {
  const currentEvents = events;

  function showName(): void {
    return console.log(`create{{ pascalCase name }}`);
  }

  return {
    showName,
  };
}

export { showName };