import * as React from 'react';

import { Wrapped, Text } from '../../../components';

import { I{{ pascalCase name }}Layout } from '../data'

export const {{pascalCase name}}: React.FC<I{{pascalCase name}}Layout> = ({ data }) => (
  <Wrapped flex={1} center testID={`{{pascalCase name}}`}>
    <Text color="ORANGE">{`{{pascalCase name}}`}</Text>
  </Wrapped>
)