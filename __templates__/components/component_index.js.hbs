import React from 'react';
import { I{{ pascalCase name }} } from './data';
import { {{ pascalCase name }} as Layout } from './Layout';

export const {{ pascalCase name }}: React.FC<I{{ pascalCase name }}> = props => <Layout {...props} />;