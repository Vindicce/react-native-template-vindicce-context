import React from 'react';
import { {{ pascalCase name }} } from './{{ pascalCase name }}'
// import views here
import { createStackNavigator } from '@react-navigation/stack';

const {{ pascalCase name }}Stack = createStackNavigator();

const views = {
  {{ pascalCase name }},
  // add viewsName here
};

const allViews: { [key: string]: any } = {
  ...views,
};

function defineRoutesProps(name: string) {
  return {
    name,
    options: { headerShown: false },
    component: allViews[name],
  };
}

const viewsNames = Object.keys(views);

export default function {{ pascalCase name }}Routes() {
  return (
    <{{ pascalCase name }}Stack.Navigator>
      {viewsNames.map(viewName => (
        <{{ pascalCase name }}Stack.Screen key={viewName} {...defineRoutesProps(viewName)} />
      ))}
    </{{ pascalCase name }}Stack.Navigator>
  )
}
