import React from 'react';
import C from './styles';
import { I{{ pascalCase name }}Layout } from '../data'
import { External } from '../../../../components'

const {{pascalCase name}}: React.FC<I{{ pascalCase name }}Layout> = ({ data }) => (
  <External>
    <C.Container>
      <C.Text>{`{{pascalCase name}}`}</C.Text>
    </C.Container>
  </External>
);

export default {{pascalCase name}};