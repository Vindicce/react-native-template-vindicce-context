import React from 'react';
import data from './data';
import Layout from './Layout';

const {{pascalCase name}}: React.FC = props => <Layout {...props} data={data} />;

export default {{pascalCase name}};